# -AIDA-TA-Terminal-Assistant 🤖
AIDA TA - это интерфейс для работы с языковыми моделями Hugging Face, сфокусированный главным образом на задачах программирования, позволяющий через конфигурационный файл адаптировать систему под любую модель, параметры генерации и аппаратные возможности. Проект решает проблему сложности настройки и интеграции AI-моделей для задач программирования.
Чтобы изменить узкую специализацию на программировании, нужно будет изменить системные промпты, специализированные методы и пользовательский интерфейс.
<!-- Badges -->

<p align="center">
<img alt="Python" src="https://img.shields.io/badge/Python-3.8%2B-blue?style=for-the-badge&logo=python">
<img alt="License" src="https://img.shields.io/badge/License-MIT-green?style=for-the-badge">
<img alt="Status" src="https://img.shields.io/badge/Status-Active-brightgreen?style=for-the-badge">
</p>

## 🎯 Проблема и Решение

![Проблема](https://img.shields.io/badge/Проблема-red) 

Локальное развертывание и управление LLM для генерации кода сопряжено с рядом трудностей:

**Высокие требования к ресурсам**: Многие модели требуют дорогостоящего оборудования.

**Сложность настройки**: Процесс загрузки, квантизации и настройки параметров генерации требует специальных знаний.

**Отсутствие унификации**: Разные модели требуют разного подхода к интеграции.

**Нет удобного интерфейса**: Взаимодействие с моделями часто ограничивается консолью, что неудобно для анализа и итеративной работы.

![Решение](https://img.shields.io/badge/Решение-green) 

AIDA решает эти проблемы, предоставляя:

**Унифицированный интерфейс**: Веб-интерфейс на Streamlit для всех задач: генерации, анализа, исправления и объяснения кода.

**Гибкая конфигурация**: Единый файл config.json позволяет управлять всем: от выбора модели и устройства (CPU/GPU) до параметров генерации (temperature, top_p) и оптимизаций.

**Оптимизация ресурсов**: Встроенные механизмы квантизации (4-bit/8-bit), управления кэшем и памятью позволяют запускать даже крупные модели (например, 6.7B) на потребительском оборудовании.

**Локальность и безопасность**: Весь процесс происходит на вашей машине, что гарантирует полный контроль и конфиденциальность данных.

## ✨ Ключевые функции

| Функция                     | Описание                                                                 |
|-----------------------------|--------------------------------------------------------------------------|
| **Динамическая загрузка моделей** | Поддержка любых моделей из Hugging Face Hub. Возможность использовать локально сохраненные веса или загружать их из репозитория. |
| **Полная конфигурация**     | Управление всеми аспектами через `config.json`: от выбора модели до порогов мониторинга производительности. |
| **Многозадачность**         | Режимы для генерации, анализа, исправления и объяснения кода с поддержкой системных промптов. |
| **Интерактивный UI**        | Пользовательский интерфейс на Streamlit с вкладками, чатом, мониторингом ресурсов в реальном времени и историей сессий. |
| **Оптимизация производительности** | Поддержка `bfloat16`, 4-битного квантования, `flash-attention` и других техник для ускорения инференса. |
| **Логирование** | Логи сессий для отладки и анализа работы ассистента. |
| **Персонализация**          | Ассистент запоминает имя пользователя и ведет статистику по выполненным задачам. |

## 🔧 Технологический стек

**Python-библиотеки**: torch, transformers, streamlit, psutil, plotly, bitsandbytes

**Streamlit-компоненты**: st.tabs, st.metric, st.file_uploader

**Инфраструктура**: Локальное развертывание (CPU/GPU), опциональная интеграция с Hugging Face Hub.

## 🛠️ Конфигурация (config.json)

Конфигурация доступна по адресу

`https://alexandrd7.github.io/AIDA-TA-Terminal-Assistant/`

# 💻 Системные требования

Требования зависят от выбранной модели и настроек квантизации.

Пример для моделей Deepseek.

---

## 🧠 Модель: `deepseek-ai/deepseek-coder-1.3b-instruct` (1.3B)

### ✅ GPU (рекомендуется)
- **VRAM**:
  - `bfloat16/float16`: 4–6 GB
  - `4-битная квантизация`: ~2–3 GB
- **Подходящие видеокарты**:
  - NVIDIA GeForce GTX 1660 / RTX 2060 / 3050 и выше

### 🖥️ CPU
- **RAM**: 8–12 GB свободной оперативной памяти
- ⚠️ Производительность будет значительно ниже, чем на GPU

---

## 🧠 Модель: `deepseek-ai/deepseek-coder-6.7b-instruct` (6.7B)

### ✅ GPU (настоятельно рекомендуется)
- **VRAM**:
  - `bfloat16/float16`: 14–16 GB
  - `4-битная квантизация`: ~5–7 GB
- **Рекомендуемые видеокарты**:
  - NVIDIA GeForce RTX 3060 (12 GB), RTX 3080 / 3090 / 4070 / 4080 / 4090

### 🖥️ CPU
- **RAM**: 16–24 GB свободной оперативной памяти
- ⚠️ Запуск возможен, но будет **очень медленным**

---

## 💡 Совет

> Для работы с крупными моделями на ограниченном по мощности оборудовании используйте 4-битную квантизацию:

```json
"quantization_type": "4bit"
```

# Как начать использовать AIDA-TA-Terminal-Assistant

## ⊞ Установка на Windows

1.  Скачайте приложение **Windows Terminal** из Microsoft Store.
2.  В терминале введите команду wsl --instal
3.  Скачайте в Microsoft Store любой дистрибутив Linux (например, Ubuntu 22.04.5 LTS).
 >   Далее следуйте пунктам установки на Linux

## 🐧 Установка на Linux

1.  Установите `git` с помощью команд:
    ```bash
    sudo apt update
    sudo apt install git
    ```
2.  Клонируйте репозиторий AIDA-TA-Terminal-Assistant:
    ```bash
    git clone https://github.com/AlexandrD7/-AIDA-TA-Terminal-Assistant/
    ```

## Скачивание модели ИИ

❗ **Важно**



### Вариант 1: Скачивание вручную

1.  Скачайте модель с сайта [Hugging Face](https://huggingface.co/).
2.  Поместите её в папку, путь к которой вы укажете в конфигурации.
3.  Путь при скачивании с помощью приложения: `cache_dir` в файле `config.json`.

**Настройки системы:**
```python
    "cache_dir": "./model_cache",
```
> **Важно:** Файлы `Aida.py`, `config.json` и `Streamlit.py` должны находиться в одной директории для корректной работы приложения.

####  Скачивание модели вручную (если не установлен python или pip, смотри ниже пункт `📦 Установка зависимостей Python`)

Установка пакета `huggingface_hub` через pip:

> ```bash
> pip install huggingface_hub
> ```

Проверка, что всё установилось

>```bash
>huggingface-cli --help
>```
 Если команда распознаётся — всё работает.

 #### Команда для скачивания
 >```bash
 > huggingface-cli download codellama/CodeLlama-7b-Instruct-hf \
 >--local-dir ./models/CodeLlama-7b-Instruct-hf
 >```
Эта команда скачает все файлы модели `codellama/CodeLlama-7b-Instruct-hf` в папку `./models/CodeLlama-7b-Instruct-hf`.

### 🔧 Вариант 2: Автоматическая установка

1. Откройте файл `config.json`.

2. Впишите нужную вам модель, указав параметры:
   - `name` — имя модели на Hugging Face
   - `description` — читаемое описание модели
   - `size` — размер модели (для UI)
   - `local_path` — директория, куда будет скачана модель
   - `cache_dir` - директория, куда непосредственно будет скачана модель
   - Дополнительно: `context_size`, `use_local`, `fallback_to_remote`, `integrity_check`

3. Пример конфигурации модели:

```json
"model": {
  "name": "deepseek-ai/deepseek-coder-6.7b-instruct",
  "description": "DeepSeek Coder 6.7B Instruct",
  "size": "6.7B",
  "context_size": 4096,
  "local_path": "./models/",
  "use_local": true,
  "fallback_to_remote": true,
  "integrity_check": true
}
```
> 💡 **Совет:**  
> При первом запуске модель будет автоматически скачана и сохранена в папке, указанной в параметре настроек системы `cache_dir` (см. выше пункт **Настройки системы:** ).

### 📦 Установка зависимостей Python

1. Установите все зависимости, указанные в `requirements.txt`:

```bash
pip install -r requirements.txt
```
> ❗ **Если Python не установлен:**  
> Установите его вместе с pip, выполнив в терминале:

```bash
sudo apt update
sudo apt install python3 python3-pip
```
### 🚀 Запуск приложения

Работа может производиться как в **терминале**, так и в **браузере** (если используется web-интерфейс).

#### 🔧 Запуск в терминале:

```bash
python3 Aida.py
```
> 📁 **Убедитесь:**  
> Файлы `Aida.py`, `config.json` должны находиться в **одной директории** для корректной работы приложения.

#### 🔧 Запуск в браузере:
```bash
streamlit run Streamlit.py
```
> 📁 **Убедитесь:**  
> Файлы `Aida.py`, `config.json`, `Streamlit.py` должны находиться в **одной директории** для корректной работы приложения.

**Aida** сохраняет логи в директории **aida_logs**, которая будет доступна в той же директории, где вы запустили приложение.

